package com.igttestproject.stanislavkinzl.tabtest.mvp.repository.di

import dagger.Module

@Module
class ComicsRepositoryModule {
/*
    @Provides
    @Singleton
    @Remote
    internal fun provideRemoteComicDataSource(apiService: ApiService,
                                              hashCalculator: HashCalculator,
                                              timeStampProvider: TimeStampProvider): ComicDataSource {
        return RemoteComicDataSource(apiService, hashCalculator, timeStampProvider)
    }

    @Provides
    @Singleton
    @Local
    internal fun provideLocalComicDataSource(mapper: ComicRealmMapper): ComicDataSource {
        return LocalComicDataSource(mapper)
    }

    @Provides
    @Singleton
    @Local
    internal fun provideLocalComicDataPersistence(): ComicDataPersistence {
        return LocalComicDataPersistence()
    }*/
}